<UserControl x:Class="FalcaPOS.Denomination.View.DenominationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FalcaPOS.Denomination.View"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
            xmlns:behavior="clr-namespace:FalcaPOS.Common.Behaviors;assembly=FalcaPOS.Common"
            xmlns:converter="clr-namespace:FalcaPOS.Common.Converters;assembly=FalcaPOS.Common"
             xmlns:prism="http://prismlibrary.com/"
            prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="stylelabel" TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="{DynamicResource Poppins}"/>
            <Setter Property="Margin" Value="5 5 0 0"></Setter>
        </Style>
        <Style x:Key="IconMargin" TargetType="materialDesign:PackIcon">
            <Setter Property="Margin" Value="0,20,0,0" />
        </Style>
        <converter:ArrayToVisibilityConverter x:Key="ArrayToVisibilityConverter"/>
        <SolidColorBrush x:Key="MaterialCardColour" Color="#F5F5F5"/>
    </UserControl.Resources>
    
        <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <!--<Grid  Margin="0 5 0 0 ">-->
        <Label FontSize="14" FontWeight="Bold" Grid.Row="0" Grid.Column="0" Margin="0 -2 0 0" Content="Denomination Summary" ></Label>
        <Label AutomationProperties.AutomationId="lblEODTodaysDateId" FontSize="12" FontWeight="Bold" Grid.Column="0" Grid.Row="1" Foreground="LightGray" Content="{Binding FormattedCurrentDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Width="40" materialDesign:ButtonAssist.CornerRadius="8" AutomationProperties.AutomationId="btnEODRefresh1Id" Margin="70 0 0 0" ToolTip="Refresh" HorizontalContentAlignment="Right" BorderThickness="0" Grid.Column="2" Grid.Row="0" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}" Command="{Binding RefreshTodaySalesCommand}"  
                    Style="{StaticResource RoundButton}">
            <Button.Content>
                <materialDesign:PackIcon Kind="Refresh"></materialDesign:PackIcon>
            </Button.Content>
        </Button>
        <materialDesign:Card Grid.Row="2" Grid.ColumnSpan="3" materialDesign:ShadowAssist.ShadowEdges="None" Background ="#252572" Margin="0,2,0,5">
            </materialDesign:Card>
            <Label FontSize="14" Foreground="White" FontWeight="Medium" VerticalAlignment="Center" Margin="10 0 0 0"  Grid.Column="0" Grid.Row="2" Content="Particular"></Label>
        <Label HorizontalContentAlignment="Left" FontSize="14" FontWeight="Medium" VerticalAlignment="Center" Margin="10 0 0 0" Foreground="White" Grid.Column="2" Grid.Row="2" Content="Amount"></Label>
            
        <materialDesign:Card Grid.Row="3" Grid.ColumnSpan="3" Margin="0 2 0 5" Background="{StaticResource MaterialCardColour}" materialDesign:ShadowAssist.ShadowEdges="None" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource stylelabel}"  Grid.Column="0" HorizontalAlignment="Left" Grid.Row="3"  Content="OPENING CASH BALANCE"></Label>
                <materialDesign:PackIcon Kind="Rupee" Grid.Column="2" Margin="0 12 0 0" Style="{StaticResource IconMargin}"/>
                <StackPanel Grid.Column="2" Margin="10 0 0 0"  Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBox x:Name="txtopening" Margin="20 0 0 0" Grid.Column="2"  MaxLength="7" Visibility="{Binding IsNewStore,Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Left" VerticalAlignment="Center" Width="100" FontSize="14" 
                                 Text="{Binding DenominationModel.OpeningCash,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="KeyUp">
                                <i:InvokeCommandAction Command="{Binding EnterOpeningCashBalanceCommand}" CommandParameter="{Binding ElementName=txtopening}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <i:Interaction.Behaviors>
                            <behavior:TextBoxInputRegExBehaviour EmptyValue="0" RegularExpression="^[0-9]*$" />
                        </i:Interaction.Behaviors>
                    </TextBox>
                    <Label Grid.Column="2" AutomationProperties.AutomationId="lblEODOpeningCashBalanceId" Visibility="{Binding DenominationModel.IsOpeningCash,Converter={StaticResource InverseBoolToVisConverter}}" HorizontalAlignment="Left" Margin="15 5 0 0"  Width="100" FontSize="14" Content="{Binding DenominationModel.OpeningCash}">
                    </Label>
                </StackPanel>
                <!--<Label Grid.Column="2" FontFamily="14" Width="100" Margin="0 5 0 0" HorizontalAlignment="Left" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource InverseBoolToVisConverter}}"  Style="{StaticResource stylelabel}"  Content="{Binding DenominationModel.OpeningCash}"></Label>-->
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="4" Grid.ColumnSpan="3" materialDesign:ShadowAssist.ShadowEdges="None" Margin="0 2 0 5" Background="{StaticResource MaterialCardColour}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource stylelabel}" Grid.Column="0" HorizontalAlignment="Left" Grid.Row="4" Content="TOTAL SALES FOR THE DAY"></Label>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Column="2" Grid.Row="4">
                    <materialDesign:PackIcon Kind="Rupee" Margin="0 12 0 0" Style="{StaticResource IconMargin}"/>
                    <Label HorizontalAlignment="Left" AutomationProperties.AutomationId="lblEODTotalSalesForTheDayId" Margin="10 5 0 0" Style="{StaticResource stylelabel}" Content="{Binding DenominationModel.TotalSales}"></Label>
                    <!--<Button Margin="10 0 0 0" BorderThickness="0" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}" Command="{Binding RefreshTodaySalesCommand}"  Style="{StaticResource MaterialDesignOutlinedButton}">
                        <Button.Content>
                            <materialDesign:PackIcon Kind="Refresh"></materialDesign:PackIcon>
                        </Button.Content>
                    </Button>-->
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="5" Grid.ColumnSpan="3" Margin="0 2 0 5" materialDesign:ShadowAssist.ShadowEdges="None" Background="{StaticResource MaterialCardColour}" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource stylelabel}" Grid.Column="0" HorizontalAlignment="Left" Grid.Row="5" Content="CASH SALES"></Label>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Column="2" Grid.Row="5">
                    <materialDesign:PackIcon Kind="Rupee" Margin="0 12 0 0" Style="{StaticResource IconMargin}"/>
                    <Label HorizontalAlignment="Left" AutomationProperties.AutomationId="lblEODCashSalesId" Margin="10 5 0 0" Style="{StaticResource stylelabel}"  Content="{Binding DenominationModel.Cash}"></Label>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="6" Grid.ColumnSpan="3" materialDesign:ShadowAssist.ShadowEdges="None" Margin="0 2 0 5" Background="{StaticResource MaterialCardColour}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource stylelabel}" Grid.Column="0" HorizontalAlignment="Left" Grid.Row="6" Content="UPI SALES"></Label>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Column="2" Grid.Row="6">
                    <materialDesign:PackIcon Kind="Rupee" HorizontalAlignment="Right" Margin="0 12 0 0" Style="{StaticResource IconMargin}"/>
                    <Label HorizontalAlignment="Left"  AutomationProperties.AutomationId="lblEODUPISalesId" Margin="10 5 0 0" Style="{StaticResource stylelabel}"  Content="{Binding DenominationModel.UPI}"></Label>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="7" Grid.ColumnSpan="3" Margin="0 2 0 5" materialDesign:ShadowAssist.ShadowEdges="None" Background="{StaticResource MaterialCardColour}" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource stylelabel}" Grid.Column="0" HorizontalAlignment="Left" Grid.Row="7" Content="CREDIT SALES"></Label>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Column="2" Grid.Row="7">
                    <materialDesign:PackIcon Kind="Rupee"  Margin="0 12 0 0" Style="{StaticResource IconMargin}"/>
                    <Label HorizontalAlignment="Left" AutomationProperties.AutomationId="lblEODCreditSalesId" Margin="10 5 0 0" Style="{StaticResource stylelabel}"  Content="{Binding DenominationModel.Credit}"></Label>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="8" Grid.ColumnSpan="3" Margin="0 2 0 5" materialDesign:ShadowAssist.ShadowEdges="None" Background="{StaticResource MaterialCardColour}" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource stylelabel}" Grid.Column="0" HorizontalAlignment="Left" Grid.Row="8" Content="AVAILABLE CASH BALANCE"></Label>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Column="2" Grid.Row="8">
                    <materialDesign:PackIcon Kind="Rupee" Margin="0 12 0 0" Style="{StaticResource IconMargin}"/>
                    <Label HorizontalAlignment="Left" Margin="10 5 0 0" AutomationProperties.AutomationId="lblEODAvailableCashBalanceId" Style="{StaticResource stylelabel}"  Content="{Binding DenominationModel.AvailableCash}"></Label>
                </StackPanel>
            </Grid>
        </materialDesign:Card>


        <materialDesign:Card Grid.Row="9"  Grid.ColumnSpan="3" Margin="0 0 60 5" materialDesign:ShadowAssist.ShadowEdges="None" Background="{StaticResource MaterialCardColour}" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label Style="{StaticResource stylelabel}" Grid.Column="0" HorizontalAlignment="Left" Grid.Row="9" Content="BANK DEPOSIT"></Label>

                <StackPanel Orientation="Horizontal" Margin="39 0 0 0" Grid.Column="2" Grid.Row="9">

                    <materialDesign:PackIcon Kind="Rupee" Grid.Column="2" Grid.Row="9" Margin="0 12 0 0" Style="{StaticResource IconMargin}"/>
                    <Label x:Name="txtdepost"  AutomationProperties.AutomationId="lblEODBandDepositId" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Left" Margin="10 5 0 0" VerticalAlignment="Center" Width="70" FontSize="14"  
                             Content="{Binding DenominationModel.Deposit,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                        <!--<i:Interaction.Triggers>
                            <i:EventTrigger EventName="KeyUp">
                                <i:InvokeCommandAction Command="{Binding EnterDayDepostiCommand}" CommandParameter="{Binding ElementName=txtdepost}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>-->
                        <!--<i:Interaction.Behaviors>
                            <behavior:TextBoxInputRegExBehaviour EmptyValue="0" RegularExpression="^[0-9]*$" />
                        </i:Interaction.Behaviors>-->
                    </Label>
                    <!--<Button Margin="0 0 0 0" BorderThickness="0" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}" Command="{Binding DepositeAttanchemntCommad}" CommandParameter="{Binding}"  Style="{StaticResource MaterialDesignOutlinedButton}">
                        <Button.Content>
                            <materialDesign:PackIcon Kind="Attachment"></materialDesign:PackIcon>
                        </Button.Content>
                    </Button>-->
                    <Label   Style="{StaticResource stylelabel}" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource InverseBoolToVisConverter}}" HorizontalAlignment="Left" Margin="10 5 0 0"  Content="{Binding DenominationModel.Deposit}"></Label>
                    <!--<StackPanel  Visibility="{Binding DenominationModel.Deposit,Converter={StaticResource NotZeroToVisibilityConverter}}">
                    <Button Margin="0 0 0 0" BorderThickness="0" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource InverseBoolToVisConverter}}" Command="{Binding DownloadDenominationCommand}" CommandParameter="{Binding}"  Style="{StaticResource MaterialDesignOutlinedButton}">
                        <Button.Content>
                            <materialDesign:PackIcon Kind="Download"></materialDesign:PackIcon>
                        </Button.Content>
                    </Button>
                </StackPanel>-->
                </StackPanel>

            </Grid>
            </materialDesign:Card>
        <Button AutomationProperties.AutomationId="btnEODAddDepositId" Height="22" FontSize="22" Grid.Row="9" Grid.Column="2" Margin="220 0 0 0" HorizontalAlignment="Right" Visibility="{Binding IsStoreVisibile,Converter={StaticResource InverseBoolToVisConverter}}" Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type local:DenominationView}}, Path=DataContext.AddDepositCommad}"  CommandParameter="{Binding}"  Style="{DynamicResource ActionButton}">
            <Button.Content>
                <materialDesign:PackIcon Kind="Plus"></materialDesign:PackIcon>
            </Button.Content>
        </Button>



        <!-- Sales Return Cash to Be moved-->
            <!--<materialDesign:Card Grid.Row="10" Grid.ColumnSpan="3" Margin="0 3 0 0" Background="{StaticResource MaterialCardColour}" ></materialDesign:Card>
                <Label Style="{StaticResource stylelabel}" Grid.Column="0" HorizontalAlignment="Left" Grid.Row="10" Content="SALES RETURN CASH"></Label>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Column="2" Grid.Row="10">
                    <materialDesign:PackIcon Kind="Rupee" Margin="0 12 0 0" Style="{StaticResource IconMargin}"/>
                    <TextBox x:Name="txtsalesreturn" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}"
                             Margin="5 0 0 0" MaxLength="7" HorizontalAlignment="Left" VerticalAlignment="Center" Width="100" FontSize="14"  
                             Text="{Binding DenominationModel.SalesReturnCash,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                             AutomationProperties.AutomationId="txtEODAddSalesReturnCashId"
                             >
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="KeyUp">
                                <i:InvokeCommandAction Command="{Binding EnterDaySalesReturnCashCommand}" CommandParameter="{Binding ElementName=txtsalesreturn}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <i:Interaction.Behaviors>
                            <behavior:TextBoxInputRegExBehaviour EmptyValue="0" RegularExpression="^[0-9]*$" />
                        </i:Interaction.Behaviors>
                    </TextBox>
                    <Label  Style="{StaticResource stylelabel}" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource InverseBoolToVisConverter}}"  Content="{Binding DenominationModel.SalesReturnCash}"></Label>
                </StackPanel>-->

                <!--<materialDesign:Card Grid.Row="11"  Grid.ColumnSpan="3" Margin="0 3 0 0"></materialDesign:Card>-->
            <Label Style="{StaticResource stylelabel}"  HorizontalAlignment="Right" FontSize="12" Grid.Row="10" Grid.Column="1" Foreground="{StaticResource MahApps.Brushes.Accent}" Content="Closing Cash Balance"></Label>
            <StackPanel  Orientation="Horizontal" Grid.Row="10" Grid.Column="2" >
                <materialDesign:PackIcon Kind="Rupee" HorizontalAlignment="Left" Height="25" Width="25" Margin="0 4 0 0" Foreground="{StaticResource MahApps.Brushes.Accent}" Style="{StaticResource IconMargin}"/>
                    <Label FontSize="30" Height="50" AutomationProperties.AutomationId="lblEODClosingCashBalanceId" Margin="0 -10 0 0" Style="{StaticResource stylelabel}" FontWeight="Bold" Foreground="{StaticResource MahApps.Brushes.Accent}" Content="{Binding DenominationModel.ClosingCash}"></Label>
            <Button AutomationProperties.AutomationId="btnEODAddDenominationId" FontSize="20" Height="22" Visibility="{Binding IsStoreVisibile,Converter={StaticResource InverseBoolToVisConverter}}" Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type local:DenominationView}}, Path=DataContext.AddDenominationCountCommand}"  CommandParameter="{Binding}"  Margin="0 -10 10 0" HorizontalAlignment="Left" Style="{DynamicResource ActionButton}" >
                <Button.Content>
                    <materialDesign:PackIcon Kind="Plus"></materialDesign:PackIcon>
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>

            <!--<Grid Grid.Row="0" Grid.Column="1" Margin="5 5 0 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="40"/>
                   
                </Grid.RowDefinitions>
                    <Label HorizontalContentAlignment="Center" FontSize="14" FontWeight="Bold" Grid.Column="0" Grid.Row="0" Content="PARTICULARS"></Label>
                    <Label  HorizontalContentAlignment="Center" FontSize="14" FontWeight="Bold" Grid.Column="1" Grid.Row="0" Content="COUNT"></Label>
                    <Label  HorizontalContentAlignment="Center" FontSize="14" FontWeight="Bold" Grid.Column="2" Grid.Row="0" Margin="0 0 20 0" Content="TOTAL"></Label>
                   
            <materialDesign:Card Grid.Row="1" Grid.ColumnSpan="3" Margin="0 2 0 0"></materialDesign:Card>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Column="0" Grid.Row="1">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Center"  Margin="0 5 0 0" FontSize="16"  Content="2000"></Label>
            </StackPanel>


            <TextBox x:Name="txt2000" MaxLength="4"  Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Width="100"  Grid.Column="1" Grid.Row="1" Text="{Binding DenominationModel.notes_2000,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" 
                         AutomationProperties.AutomationId="txtEODAdd2000CountId">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="KeyUp">
                        <i:InvokeCommandAction Command="{Binding EnterTxtChangeCount2000Command}" CommandParameter="{Binding ElementName=txt2000}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <i:Interaction.Behaviors>
                    <behavior:TextBoxInputRegExBehaviour EmptyValue="0" RegularExpression="^[0-9]*$" />
                </i:Interaction.Behaviors>
            </TextBox>

            <Label  Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource InverseBoolToVisConverter}}"  HorizontalContentAlignment="Center" FontSize="14" Width="100"  Grid.Column="1" Grid.Row="1"  Content="{Binding DenominationModel.notes_2000}">
            </Label>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="20 0 0 0" Grid.Column="2" Grid.Row="1">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label AutomationProperties.AutomationId="lblEODTotal2000Id" HorizontalContentAlignment="Left" Width="80"  Margin="0 5 0 0" FontSize="16"  Content="{Binding DenominationModel.Totalin2000}"></Label>
            </StackPanel>

            <materialDesign:Card Grid.Row="2" Grid.ColumnSpan="3" Margin="0 2 0 0"></materialDesign:Card>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Column="0" Grid.Row="2">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Center"  Margin="0 5 0 0" FontSize="16"  Content="500"></Label>
            </StackPanel>


            <TextBox x:Name="txt500"  Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}" MaxLength="4" 
                         AutomationProperties.AutomationId="txtEODAdd500CountId"
                        HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Width="100"  Grid.Column="1" Grid.Row="2" Text="{Binding DenominationModel.notes_500,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="KeyUp">
                        <i:InvokeCommandAction Command="{Binding EnterTxtChangeCount500Command}" CommandParameter="{Binding ElementName=txt500}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <i:Interaction.Behaviors>
                    <behavior:TextBoxInputRegExBehaviour EmptyValue="0" RegularExpression="^[0-9]*$" />
                </i:Interaction.Behaviors>
            </TextBox>

            <Label AutomationProperties.AutomationId="lblEODTotal500Id" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource InverseBoolToVisConverter}}"  HorizontalContentAlignment="Center" FontSize="14" Width="100"  Grid.Column="1" Grid.Row="2"  Content="{Binding DenominationModel.notes_500}"></Label>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="20 0 0 0" Grid.Column="2" Grid.Row="2">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Left" Width="80" Margin="0 5 0 0" FontSize="16"  Content="{Binding DenominationModel.Totalin500}"></Label>
            </StackPanel>

            <materialDesign:Card Grid.Row="3" Grid.ColumnSpan="3" Margin="0 2 0 0"></materialDesign:Card>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Column="0" Grid.Row="3">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Center"  Margin="0 5 0 0" FontSize="16"  Content="200"></Label>
            </StackPanel>

            <TextBox x:Name="txt200" 
                         AutomationProperties.AutomationId="txtEODAdd200CountId"
                    Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}" MaxLength="4"  HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Width="100"  Grid.Column="1" Grid.Row="3" Text="{Binding DenominationModel.notes_200,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="KeyUp">
                        <i:InvokeCommandAction Command="{Binding EnterTxtChangeCount200Commands}" CommandParameter="{Binding ElementName=txt200}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <i:Interaction.Behaviors>
                    <behavior:TextBoxInputRegExBehaviour EmptyValue="0" RegularExpression="^[0-9]*$" />
                </i:Interaction.Behaviors>
            </TextBox>

            <Label AutomationProperties.AutomationId="lblEODTotal200Id" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource InverseBoolToVisConverter}}"  HorizontalContentAlignment="Center" FontSize="14" Width="100"  Grid.Column="1" Grid.Row="3"  Content="{Binding DenominationModel.notes_200}">
            </Label>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="20 0 0 0" Grid.Column="2" Grid.Row="3">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Left" Width="80"  Margin="0 5 0 0" FontSize="16"  Content="{Binding DenominationModel.Totalin200}"></Label>
            </StackPanel>



            <materialDesign:Card Grid.Row="4" Grid.ColumnSpan="3" Margin="0 2 0 0"></materialDesign:Card>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Column="0" Grid.Row="4">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Center"  Margin="0 5 0 0" FontSize="16"  Content="100"></Label>
            </StackPanel>

            <TextBox 
                         AutomationProperties.AutomationId="txtEODAdd100CountId"
                    x:Name="txt100" MaxLength="4"  Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Width="100"  Grid.Column="1" Grid.Row="4" Text="{Binding DenominationModel.notes_100,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="KeyUp">
                        <i:InvokeCommandAction Command="{Binding EnterTxtChangeCount100Command}" CommandParameter="{Binding ElementName=txt100}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <i:Interaction.Behaviors>
                    <behavior:TextBoxInputRegExBehaviour EmptyValue="0" RegularExpression="^[0-9]*$" />
                </i:Interaction.Behaviors>
            </TextBox>

            <Label AutomationProperties.AutomationId="lblEODTotal100Id" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource InverseBoolToVisConverter}}"  HorizontalContentAlignment="Center" FontSize="14" Width="100" Grid.Column="1" Grid.Row="4" Content="{Binding DenominationModel.notes_100}"></Label>
            <StackPanel Orientation="Horizontal" Margin="20 0 0 0" HorizontalAlignment="Center" Grid.Column="2" Grid.Row="4">
                <materialDesign:PackIcon Kind="Rupee"  Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Left"  Width="80" Margin="0 5 0 0" FontSize="16"  Content="{Binding DenominationModel.Totalin100}"></Label>
            </StackPanel>

            <materialDesign:Card Grid.Row="5" Grid.ColumnSpan="3" Margin="0 2 0 0"></materialDesign:Card>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Column="0" Grid.Row="5">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Center"  Margin="0 5 0 0" FontSize="16"  Content="50"></Label>
            </StackPanel>

            <TextBox x:Name="txt50"
                         AutomationProperties.AutomationId="txtEODAdd50CountId"
                         Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}" MaxLength="4"  HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Width="100"  Grid.Column="1" Grid.Row="5" Text="{Binding DenominationModel.notes_50,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="KeyUp">
                        <i:InvokeCommandAction Command="{Binding EnterTxtChangeCount50Command}" CommandParameter="{Binding ElementName=txt50}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <i:Interaction.Behaviors>
                    <behavior:TextBoxInputRegExBehaviour EmptyValue="0" RegularExpression="^[0-9]*$" />
                </i:Interaction.Behaviors>
            </TextBox>
            <Label AutomationProperties.AutomationId="lblEODTotal50Id" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource InverseBoolToVisConverter}}" HorizontalContentAlignment="Center" FontSize="14" Width="100" Grid.Column="1" Grid.Row="5"   Content="{Binding DenominationModel.notes_50}"></Label>
            <StackPanel Orientation="Horizontal" Margin="20 0 0 0" HorizontalAlignment="Center" Grid.Column="2" Grid.Row="5">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Left"  Width="80" Margin="0 5 0 0" FontSize="16"  Content="{Binding DenominationModel.Totalin50}"></Label>
            </StackPanel>


            <materialDesign:Card Grid.Row="6" Grid.ColumnSpan="3" Margin="0 2 0 0"></materialDesign:Card>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Column="0" Grid.Row="6">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Center"  Margin="0 5 0 0" FontSize="16"  Content="20"></Label>
            </StackPanel>

            <TextBox x:Name="txt20"
                         AutomationProperties.AutomationId="txtEODAdd20CountId"
                         Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}" MaxLength="5"  HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Width="100" Grid.Column="1" Grid.Row="6"   Text="{Binding DenominationModel.notes_20,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="KeyUp">
                        <i:InvokeCommandAction Command="{Binding EnterTxtChangeCount20Command}" CommandParameter="{Binding ElementName=txt20}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <i:Interaction.Behaviors>
                    <behavior:TextBoxInputRegExBehaviour EmptyValue="0" RegularExpression="^[0-9]*$" />
                </i:Interaction.Behaviors>
            </TextBox>

            <Label AutomationProperties.AutomationId="lblEODTotal20Id" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource InverseBoolToVisConverter}}" HorizontalContentAlignment="Center" FontSize="14" Width="100" Grid.Column="1" Grid.Row="6"   Content="{Binding DenominationModel.notes_20}"></Label>
            <StackPanel Orientation="Horizontal" Margin="20 0 0 0" HorizontalAlignment="Center" Grid.Column="2" Grid.Row="6">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Left" Width="80"  Margin="0 5 0 0" FontSize="16"  Content="{Binding DenominationModel.Totalin20}"></Label>
            </StackPanel>


            <materialDesign:Card Grid.Row="7" Grid.ColumnSpan="3" Margin="0 2 0 0"></materialDesign:Card>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Column="0" Grid.Row="7">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Center"  Margin="0 5 0 0" FontSize="16"  Content="10"></Label>
            </StackPanel>

            <TextBox x:Name="txt10" 
                         AutomationProperties.AutomationId="txtEODAdd10CountId"
                         Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}"  MaxLength="5"  HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Width="100" Grid.Column="1" Grid.Row="7"  Text="{Binding DenominationModel.notes_10,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="KeyUp">
                        <i:InvokeCommandAction Command="{Binding EnterTxtChangeCount10Command}" CommandParameter="{Binding ElementName=txt10}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <i:Interaction.Behaviors>
                    <behavior:TextBoxInputRegExBehaviour EmptyValue="0" RegularExpression="^[0-9]*$" />
                </i:Interaction.Behaviors>
            </TextBox>

            <Label AutomationProperties.AutomationId="lblEODTotal10Id" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource InverseBoolToVisConverter}}" HorizontalContentAlignment="Center" FontSize="14" Width="100" Grid.Column="1" Grid.Row="7"  Content="{Binding DenominationModel.notes_10}"></Label>
            <StackPanel Orientation="Horizontal" Margin="20 0 0 0" HorizontalAlignment="Center" Grid.Column="2" Grid.Row="7">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Left" Width="80"  Margin="0 5 0 0" FontSize="16"  Content="{Binding DenominationModel.Totalin10}"></Label>
            </StackPanel>


            <materialDesign:Card Grid.Row="8" Grid.ColumnSpan="3" Margin="0 2 0 0" ></materialDesign:Card>
            <Label  HorizontalContentAlignment="Center" FontSize="14" Grid.Column="0" Grid.Row="8" Content="Coins"></Label>
            <TextBox x:Name="txtcoins" 
                         AutomationProperties.AutomationId="txtEODAddCoinCountId"
                         Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource BooleanToVisibilityConverter}}"  MaxLength="6"  HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Width="100"  Grid.Column="1" Grid.Row="8"   Text="{Binding DenominationModel.Coins,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="KeyUp">
                        <i:InvokeCommandAction Command="{Binding EnterTxtChangeCountCoinsCommand}" CommandParameter="{Binding ElementName=txtcoins}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <i:Interaction.Behaviors>
                    <behavior:TextBoxInputRegExBehaviour EmptyValue="0" RegularExpression="^[0-9]*$" />
                </i:Interaction.Behaviors>
            </TextBox>
            <Label AutomationProperties.AutomationId="lblEODTotalCoinId" Visibility="{Binding DenominationModel.IsRole,Converter={StaticResource InverseBoolToVisConverter}}" HorizontalContentAlignment="Center" FontSize="14" Width="100"  Grid.Column="1" Grid.Row="8"  Content="{Binding DenominationModel.Coins}" ></Label>
            <StackPanel Orientation="Horizontal" Margin="20 0 0 0" HorizontalAlignment="Center" Grid.Column="2" Grid.Row="8">
                <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                <Label  HorizontalContentAlignment="Left" Width="80"  Margin="0 5 0 0" FontSize="16"  Content="{Binding DenominationModel.TotalinCoins}"></Label>
            </StackPanel>


                    <materialDesign:Card Grid.Row="9" Grid.ColumnSpan="3" Margin="0 2 0 0" ></materialDesign:Card>
                    <Label  HorizontalContentAlignment="Center" FontWeight="Bold" FontSize="16" Grid.Column="0" Grid.Row="9" Content="Total"></Label>
                    <StackPanel Orientation="Horizontal"  Margin="40 0 0 0"  Grid.Column="2" Grid.Row="9">
                        <materialDesign:PackIcon Kind="Rupee" Margin="0 15 0 0" Style="{StaticResource IconMargin}"/>
                    <Label   FontWeight="Bold"  Margin="0 5 0 0" FontSize="16"  Content="{Binding DenominationModel.Total,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"></Label>
                    </StackPanel>

                </Grid>-->
       
    <!--</Grid>-->
</UserControl>
